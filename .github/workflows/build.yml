---
name: Build container image

on:
  workflow_run:
    workflows: ["Test Zerith"]
    types: [completed]

  schedule:
    - cron: '05 10 * * *' # 10:05 UTC daily

  workflow_dispatch:

env:
  IMAGE_DESC: "Zerith"
  IMAGE_NAME: "${{ github.event.repository.name }}"
  IMAGE_REGISTRY: "ghcr.io/${{ github.repository_owner }}"
  DEFAULT_TAG: "latest"

concurrency:
  group: ${{ github.workflow }}-${{ github.ref || github.run_id }}-${{ inputs.brand_name}}-${{ inputs.stream_name }}
  cancel-in-progress: true

jobs:
  build_push:
    name: Build and push image

    runs-on: ubuntu-24.04

    timeout-minutes: 40

    strategy:
      fail-fast: false
      matrix:
        flavor:
          - ""
          - "nvidia"

    permissions:
      contents: read
      packages: write
      id-token: write          # keyless cosign signing via the GH OIDC token

    steps:
      - name: Change image name depending on build flavor
        env:
          BUILD_FLAVOR: ${{ matrix.flavor }}
        run: |
          if [ -n "$BUILD_FLAVOR" ]; then
            echo "IMAGE_NAME=${IMAGE_NAME}-${BUILD_FLAVOR}" >> "$GITHUB_ENV"
          fi

      - name: Lowercase image variables
        run: |
          echo "IMAGE_REGISTRY=${IMAGE_REGISTRY,,}" >> "$GITHUB_ENV"
          echo "IMAGE_NAME=${IMAGE_NAME,,}" >> "$GITHUB_ENV"

      - name: Set tag prefix
        if: github.ref_name != 'main'
        run: |
          echo "TAG_PREFIX=${{ github.ref_name }}." >> "$GITHUB_ENV"

      - name: Checkout
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v6

      - name: Get current date / version
        id: meta
        run: |
          echo "date=$(date -u +%Y-%m-%dT%H:%M:%SZ)" >> "$GITHUB_OUTPUT"
          echo "version=$(date -u +%Y%m%d)" >> "$GITHUB_OUTPUT"

      - name: Generate deploy id
        id: deploy_id
        run: echo "id=${GITHUB_SHA:0:8}$(openssl rand -hex 4)" >> "$GITHUB_OUTPUT"

      # 1. Build the rootfs image (no UKI inside it any more).
      - name: Build Image
        id: build_image
        uses: redhat-actions/buildah-build@7a95fa7ee0f02d552a32753e7414641a04307056 # v2
        with:
          containerfiles: |
            ./Containerfile
          image: ${{ env.IMAGE_NAME }}
          tags: localbuild
          build-args: |
            DEPLOY_ID=${{ steps.deploy_id.outputs.id }}
          oci: false

      # 2. Post-process the rootfs into the immutable layout (/usr/etc, factory /var).
      - name: Post-process rootfs
        run: buildah unshare scripts/ci/post-process-rootfs.sh

      # 3. Export the post-processed rootfs to a plain directory.
      - name: Export rootfs
        run: scripts/ci/export-rootfs.sh

      # 4. Render composefs, pin its digest, build + sign the UKI.
      - name: Render composefs + build/sign UKI
        env:
          DEPLOY_ID: ${{ steps.deploy_id.outputs.id }}
          SB_KEY: ${{ secrets.SB_KEY }}
          SB_CERT: ${{ secrets.SB_CERT }}
        run: scripts/ci/render-uki.sh

      # 5. Tooling for push + sign.
      # TODO: pin these to commit SHAs to match the rest of the workflow.
      - name: Install oras
        uses: oras-project/setup-oras@v2
      - name: Install cosign
        uses: sigstore/cosign-installer@v3

      - name: Login to GHCR
        if: github.event_name != 'pull_request' && github.ref == format('refs/heads/{0}', github.event.repository.default_branch)
        run: |
          echo "${{ github.token }}" | oras login ghcr.io -u "${{ github.actor }}" --password-stdin
          echo "${{ github.token }}" | cosign login ghcr.io -u "${{ github.actor }}" --password-stdin

      # 6. Write per-deployment metadata + pack objects into one blob.
      - name: Write deployment.json + pack objects
        env:
          DEPLOY_ID: ${{ steps.deploy_id.outputs.id }}
          VERSION: ${{ steps.meta.outputs.version }}
          DEPLOYED_AT: ${{ steps.meta.outputs.date }}
        run: scripts/ci/pack-objects.sh

      # 7. Gate: verify the pack + index reconstruct every object byte-for-byte
      #    before anything is pushed. Runs on every build, including PRs.
      - name: Verify pack integrity
        run: python3 scripts/ci/verify-pack.py

      # 8. Push the OCI artifacts via oras.
      - name: Push deployment + objects
        if: github.event_name != 'pull_request' && github.ref == format('refs/heads/{0}', github.event.repository.default_branch)
        run: scripts/ci/push-artifacts.sh

      # 9. Sign the deployment artifact (keyless, OIDC).
      - name: Cosign sign
        if: github.event_name != 'pull_request' && github.ref == format('refs/heads/{0}', github.event.repository.default_branch)
        env:
          COSIGN_YES: "true"
        run: scripts/ci/cosign-sign.sh
